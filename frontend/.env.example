# Frontend Environment Variables

# API Configuration
VITE_API_URL=http://localhost:8000

# Security - Encryption Key (must be same as backend)
# Generate with: openssl rand -base64 32
VITE_ENCRYPTION_KEY=ncd-system-secret-key-min-32-chars!

# Environment
VITE_ENV=production
